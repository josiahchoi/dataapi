{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "dataapi",
  "password": "dataapi",
  "database": "dataapi",
  "entities": ["src/data/**/*.entity.ts"],
  "synchronize": false,
  "migrations": ["migration/*.ts"],
  "cli": {
      "migrationsDir": "migration"
  }
}
