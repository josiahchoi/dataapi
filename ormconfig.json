{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "dataapi",
  "password": "dataapi",
  "database": "dataapi",
  "entities_old": ["dist/**/*.entity{.ts,.js}", "dist/src/**/*.entity{.ts,.js}"],
  "entities": ["src/data/**/*.entity.ts"], 
  "synchronize": false,
  "migrations_old": ["migration/*.ts"],
  "cli": {
      "migrationsDir": "migration"
  }
}
