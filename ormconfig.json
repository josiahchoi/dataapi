{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "dataapi",
  "password": "dataapi",
  "database": "dataapi",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": false,
  "migrations": ["migration/*.ts"],
  "cli": {
      "migrationsDir": "migration"
  }
}
